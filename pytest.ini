[pytest]
markers =
    unit: deterministic, offline tests (default lane, no network access)
    live_capture: opt-in tests that drive real Playwright captures against Mercari; run explicitly with RUN_LIVE_CAPTURE_TESTS=1 pytest -m live_capture
